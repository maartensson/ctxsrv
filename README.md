# Golang CTX SRV

Provides a context aware alternative for `http.ListenAndServe()` with graceful shutdown, and logging, as well as good integration with systemd, like socket activation, notify, and logging+level.
